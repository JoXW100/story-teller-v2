import { Palette } from "types/palettes"
import Cobalt from "./cobalt.json"
import Coral from "./coral.json"
import Dark from "./dark.json"

const Palettes: { [key: string]: Palette } = {
    "cobalt": Cobalt,
    "coral": Coral,
    "dark": Dark
}

export default Palettes